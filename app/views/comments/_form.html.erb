<%= form_for([@post, @post.comments.build], :remote => true) do |f| %>
  <div id="comment-body-add">
    <%= f.label :body %><br />
    <%= f.text_area :body, :required => true %>
  </div>
  <div class="actions">
    <%= f.submit "Add Comment", disable_with: "Submitting.." %>
  </div>
<% end %>
