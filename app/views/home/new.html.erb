<%= form_tag(search_path, :remote => true, :id => 'search-form') do %>
  <%= search_field_tag :search, params[:search], placeholder: "Search", required: true %>
  <div>
    <%= button_tag "Search" %>
  </div>
<% end %>
<div id="posts-data">
  <%= render 'posts_table', object: @posts %>
</div>
